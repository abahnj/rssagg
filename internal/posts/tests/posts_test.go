package tests

import (
	"testing"
)

func TestParseRSSTime(t *testing.T) {
	t.Skip("Testing the unexported parseRSSTime function requires refactoring")
	
	// This is a placeholder for testing the time parsing functionality
	// Since parseRSSTime is not exported, we'd need to either:
	// 1. Export the function or
	// 2. Test it through public methods
}

func TestCreatePost(t *testing.T) {
	t.Skip("Skipping until test mocks are implemented")
}

func TestGetPostsForUser(t *testing.T) {
	t.Skip("Skipping until test mocks are implemented")
}